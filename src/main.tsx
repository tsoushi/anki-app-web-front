import React from 'react'
import ReactDOM from 'react-dom/client'
import AnkiApp from './components/AnkiApp/AnkiApp'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <AnkiApp></AnkiApp>
  </React.StrictMode>
)
